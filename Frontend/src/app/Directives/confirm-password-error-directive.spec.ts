import { ConfirmPasswordErrorDirective } from './confirm-password-error-directive';

describe('ConfirmPasswordErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPasswordErrorDirective();
    expect(directive).toBeTruthy();
  });
});
