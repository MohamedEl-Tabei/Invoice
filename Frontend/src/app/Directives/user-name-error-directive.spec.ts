import { UserNameErrorDirective } from './user-name-error-directive';

describe('UserNameErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new UserNameErrorDirective();
    expect(directive).toBeTruthy();
  });
});
