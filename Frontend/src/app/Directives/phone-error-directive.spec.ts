import { PhoneErrorDirective } from './phone-error-directive';

describe('PhoneErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneErrorDirective();
    expect(directive).toBeTruthy();
  });
});
