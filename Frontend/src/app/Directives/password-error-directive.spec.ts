import { PasswordErrorDirective } from './password-error-directive';

describe('PasswordErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordErrorDirective();
    expect(directive).toBeTruthy();
  });
});
